i#include <stdio.h>

/**
 * main - entry point reverse print the aplhabets,
 *        only using putchar and without char variables.
 *
 * Return: Always 0 (success).
 */
int main(void)
{
	char i;

	for (i = 'z'; num >= 'a'; i--)
		putchar(i);

	putchar('\n');

	return (0);
}
